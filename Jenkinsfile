pipeline {
	agent any
	stages {
		stage('dev') {
			steps {
				echo "development"
			}
		}
		
		stage('Test') {
			steps {
				echo "Test"
			}
		}
		stage('Prod') {
			steps {
			         echo "Production"
			}
		}
	}	
}
